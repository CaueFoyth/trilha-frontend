<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
</head>
<body>
  <h1>{{message}}</h1>

  <h2>Lista de Produtos</h2>
  <ul>
    {{#each products}}
      <li>
        <strong>{{this.name}}</strong> — R$ {{this.price}}
      </li>
    {{/each}}
  </ul>

  <h2>Posts Recentes da API</h2>
  <ul>
    {{#each posts}}
      <li>
        <strong>{{this.title}}</strong>
        <p>{{this.body}}</p>
      </li>
    {{/each}}
  </ul>
</body>
</html>
