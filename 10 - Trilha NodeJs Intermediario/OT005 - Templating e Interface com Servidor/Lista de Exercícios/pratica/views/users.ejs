<%- include('partials/header', { title }) %>
<h2>Usuários Cadastrados</h2>
<ul>
  <% users.forEach(u => { %>
    <li><%= u.nome %> — <%= u.email %></li>
  <% }) %>
</ul>
<%- include('partials/footer') %>
