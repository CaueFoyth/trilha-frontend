let car = {
    brand: "Citroen",
    model: "C4 Cactus",
    year: 2019,
}

console.log(`Chaves do objeto: ${Object.keys(car).join(", ")}`);
console.log(`Valores do objeto: ${Object.values(car).join(", ")}`);